<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/setupFragment">

    <fragment
        android:id="@+id/setupFragment"
        android:name="com.vortex.downloader.ui.SetupFragment"
        android:label="Setup">
        <action
            android:id="@+id/action_setup_to_home"
            app:destination="@id/homeFragment"
            app:enterAnim="@anim/fade_in"
            app:exitAnim="@anim/fade_out"
            app:popUpTo="@id/setupFragment"
            app:popUpToInclusive="true"/>
    </fragment>

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.vortex.downloader.ui.home.HomeFragment"
        android:label="İndir"/>

    <fragment
        android:id="@+id/historyFragment"
        android:name="com.vortex.downloader.ui.history.HistoryFragment"
        android:label="Geçmiş"/>

</navigation>
